git config --global user.email "<GitHub email ID>"

git config --global user.name "<Name>"

#git init

git add <filename>

git rm --cached <file>

git status

#git remote add origin <repo url>

git push -u origin master

git clone <repo>

git log

EMEGRGENCY
============
to revert changes of a particular commit:
type:

git log

copy the commit ID of the commit you want to revert

and type:

git revert <commit ID>

Then resolve the merge conflicts.
